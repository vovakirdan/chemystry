# Backend Adapters Layout

Boundary adapters for external interaction live here.

- `ipc/` Tauri command/input-output adapters.
- `storage/` database and persistence adapters.
- `io/` import/export format adapters.

Scaffold only for E01-T03.
