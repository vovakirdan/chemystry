# Frontend App Shell Layout

Application shell wiring lives here.

- `layout/` global layout primitives.
- `providers/` app-level providers and composition roots.
- `routing/` route mapping and navigation setup.

Scaffold only for E01-T03.
