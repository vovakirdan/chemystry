# Frontend Shared Layout

Shared frontend building blocks live here.

- `components/` reusable UI components.
- `lib/` shared utilities and helpers.
- `types/` shared frontend type definitions.

Scaffold only for E01-T03.
