# E2E Test Scaffold

This directory is reserved for Playwright end-to-end coverage.

- `specs/` contains scenario specs.
- `fixtures/` contains static test data.
- `support/` contains shared E2E helpers.

Scaffold only for E01-T03; no runtime logic is implemented here.
